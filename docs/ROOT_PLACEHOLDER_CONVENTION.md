# Root placeholder convention

Operational reference JSONs in this directory may use `{{MLA_ROOT}}/...` to denote the `AIMO3-SHOWTIME/mla` root. Historical run artifacts and generated outputs are intentionally not rewritten.
